<table>
    <tr>
        <th>EVENTS</th>
       
        
    </tr>
    
    <tr>
        <th> Name of Event</th>
        <th> Date</th>
    </tr>

<% @events.each do |event| %>




    
     <tr>
         
        
        <td><%= event.name%></td>
         <td><%= event.date %></td>
          <td><%= link_to 'Show', contact_path(event) %></td>
    
     </tr>
     <%end%>
    
  </table>

   <%= link_to 'Back', contacts_path %> 